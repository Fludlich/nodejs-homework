const {resendVerifyingEmailController} = require('./resendEmail')
const {verifaingEmailController} = require('./verifyEmail')

module.exports = {
  resendVerifyingEmailController,
  verifaingEmailController,
}
